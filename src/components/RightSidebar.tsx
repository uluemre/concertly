'use client';
import { Box, Typography } from '@mui/material';

export default function RightSidebar() {
    return (
        <Box p={2}>
            <Typography variant="h6">Popular Artists & Events</Typography>
        </Box>
    );
}
